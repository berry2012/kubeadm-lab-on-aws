apiVersion: v1
kind: ConfigMap
metadata:
  name: north-html
  namespace: geo
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
    <title>North Service</title>
    <style>
    html { color-scheme: light dark; }
    body { width: 35em; margin: 0 auto;
    font-family: Tahoma, Verdana, Arial, sans-serif; }
    </style>
    </head>
    <body>
    <h1>North Service</h1>
    <p>Location: Northern Region</p>
    </body>
    </html>
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: north
  namespace: geo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: north
  template:
    metadata:
      labels:
        app: north
    spec:
      containers:
      - name: north
        image: nginx
        ports:
        - containerPort: 80
        env:
        - name: SOUTH_SERVICE_URL
          value: "http://south.geo.svc.cluster.local"
        - name: GLOBALAPP_SERVICE_URL
          value: "http://globalapp.geo.svc.cluster.local"
        resources:
          requests:
            memory: "32Mi"
            cpu: "25m"
          limits:
            memory: "64Mi"
            cpu: "50m"
        volumeMounts:
        - name: html-content
          mountPath: /usr/share/nginx/html
      volumes:
      - name: html-content
        configMap:
          name: north-html
---
apiVersion: v1
kind: Service
metadata:
  name: north
  namespace: geo
spec:
  selector:
    app: north
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
